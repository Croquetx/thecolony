/*----------------------------------------------------------------------  *	defs.h * *	Header file for animate. *	Copyright 1987 by David A. Smith. *---------------------------------------------------------------------- */#include <QuickDraw.h>#include <WindowMgr.h>#include <MenuMgr.h>#include <EventMgr.h>#include <DialogMgr.h>#include <DeskMgr.h>#include <math.h>#include <ControlMgr.h>#include <ToolboxUtil.h>#define WIDTH 416#define HEIGHT 295#define BTOP 30#define FILEMENU 256#define EDITMENU 257#define LASTMENU 3#define APPLEMENU 1#define NIL 0L#define NUMBUTS 16EXT Rect dbutton[NUMBUTS];EXT int changed;EXT MenuHandle myMenus[LASTMENU];EXT CursHandle Curs[10];EXT int refNum;EXT Ptr restartProc;EXT int doneflag;EXT EventRecord key;EXT GrafPtr theWindow;EXT GrafPtr animWindow;EXT WindowPtr bgWindow;EXT WindowPtr whichWindow;EXT WindowPtr sWindow;EXT Rect bounds;EXT char *buffer;EXT int blength;EXT GrafPort meta0,meta1;EXT Rect size;EXT Rect DClip,rBounds;EXT int pchanged;EXT int cnum;EXT int plane;EXT Pattern bg;EXT struct sprite{	PicHandle pict;	BitMap* fg;	BitMap* mask;	BitMap* bg;	Rect clip;	Rect locate;	int used;	}cSprite[128];EXT int locSprite;struct Object{	int spritenum;	int xloc,yloc;	};EXT struct oSprite{	struct Object object[8];	BitMap* bg;		/*sprite #1 background*/	long overlap;	/*each bit corresponds to what sprites are on top*/	int visible;	/*visible flag*/	int size;		/*number of sprites in this object*/	int current;	/*current visible sprite*/	int xloc,yloc;	/*base position of object*/	int acurrent; 	/*animation system current visible sprite*/	int axloc,ayloc;/*animation system base position of object*/	unsigned char container;	unsigned char locked;	unsigned char freeze;	unsigned char animate;	int size;	int interact[8];/*also acts as object numbers if container*/	}lSprite[32];EXT int maxSprite;EXT int anum;		/*current animated object*/