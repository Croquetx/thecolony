/*--------------------------------- * The Animator * Copyright 1988 by David A. Smith * All Rights Reserved * 111 Gold Meadow Dr. * Cary, NC 27513 *---------------------------------*//*----------------------------------------------------------------------  *	adefs.h * *	Header file for animate. *	Copyright 1987 by David A. Smith. *---------------------------------------------------------------------- */#include <QuickDraw.h>#include <WindowMgr.h>#include <MenuMgr.h>#include <EventMgr.h>#include <DialogMgr.h>#include <DeskMgr.h>#include <math.h>#include <ControlMgr.h>#include <ToolboxUtil.h>#define WIDTH 416#define HEIGHT 295#define BTOP 30#define FILEMENU 256#define EDITMENU 257#define LASTMENU 3#define APPLEMENU 1#define NIL 0L#define NUMBUTS 16EXT Rect dbutton[NUMBUTS];EXT int changed;EXT MenuHandle myMenus[LASTMENU];EXT CursHandle Curs[10];EXT int refNum;EXT Ptr restartProc;EXT int doneflag;EXT EventRecord key;EXT GrafPtr theWindow;EXT GrafPtr animWindow;EXT WindowPtr bgWindow;EXT WindowPtr whichWindow;EXT WindowPtr sWindow;EXT Rect bounds;EXT char *buffer;EXT int blength;EXT GrafPort meta0,meta1;EXT Rect size;EXT Rect DClip,rBounds;EXT int cnum;EXT int plane;EXT Pattern topBG;EXT Pattern newTopBG;EXT Pattern bottomBG;EXT Pattern newBottomBG;EXT int divideBG;EXT struct {	int active;	PicHandle pict;	BitMap* fg;	BitMap* mask;	Rect clip;	Rect locate;	}background;	EXT struct sprite{	PicHandle pict;	BitMap* fg;	BitMap* mask;	BitMap* bg;	Rect clip;	Rect locate;	int used;	}cSprite[128];EXT int locSprite;EXT int maxsprite;EXT ControlHandle sControl;struct Object{	int spritenum;	int xloc,yloc;	};EXT struct oSprite{	int size;		/*number of sprites in this object*/	struct Object object[8];	BitMap* bg;		/*sprite #1 background*/	int visible;	/*visible flag*/	int current;	/*current visible sprite*/	int xloc,yloc;	/*base position of object*/	int acurrent; 	/*animation system current visible sprite*/	int axloc,ayloc;/*animation system base position of object*/	unsigned char type;/*key or object*/	unsigned char frozen;	unsigned char locked;	int link; /*linked to other object?*/	int key;/*key number when acting on*/	int lock;/*lock number when being acted upon*/	}lSprite[32];EXT int maxSprite;EXT int anum;		/*current animated object*/