/*--------------------------------- * The Animator * Copyright 1988 by David A. Smith * All Rights Reserved * 111 Gold Meadow Dr. * Cary, NC 27513 *---------------------------------*/#define EXT extern #include "adefs.h"/*{char str[255]; int i=0; sprintf(str," %ld",size); while(str[i])i++;str[0]=i;Inform(str);}*//*--------------------------------------------------------------------- * Inform(str) * char *str; *  * Psuedo dialog box that informs the developer about what he can not * see. Displays a string in a box. Make sure that the string has a * \p in front of it indicating that it is a Pascal string or the length  * must be calculated directly: * *		sprintf(str," x=%d y=%d xr=%d yr=%d",myPt.h,myPt.v, *		rIcon[i].left,rIcon[i].top); *		str[0]=strlen(str); *		Inform(str); *--------------------------------------------------------------------- */Inform(str)char *str;{Rect r;WindowPtr aWindow;GrafPtr	savePort;WindowRecord wRecord;GetPort( &savePort );SetRect(&r, 152, 60, 426, 132);SetPort((aWindow = NewWindow( &wRecord , &r, "\p", 1, 1, -1L, 0, 0L)));TextFont(0);MoveTo( 4, 20 );DrawString(str);MoveTo( 4, 40 );DrawString("\pClick mouse to exit.");do { /*nothing*/ } while ( Button() );do { /*nothing*/ } while ( !Button() );CloseWindow(aWindow);SetPort(savePort);}